# F2 — Android toolchain repair in progress

Cause demonstrated: official Google repository2-3.xml provides platforms;android-37.0 revision 2, not platforms;android-37. The older repository2-1.xml does not expose this platform. The previous CI used command-line-tools 16 (12266719) and requested the nonexistent identifier. This is a tool/package-selection issue, not a reason to lower compileSdk.

Evidence: decisions/004-android-sdk-source.json records official catalogue hashes and selected package URLs/checksums. Source: https://dl.google.com/android/repository/repository2-3.xml . Official AGP 9.3 compatibility: https://developer.android.com/build/releases/agp-9-3-0-release-notes . compileSdk/targetSdk 37, AGP 9.3.0, Gradle 9.5.0 and NDK 29.0.14206865 are retained. JDK execution uses Temurin 21.0.12.1+1 to avoid the documented AGP 9.3 lint/JDK17 failure; Java source/target compatibility remains 17.

scripts/pipeline-android-setup installs official archives with pinned SHA-256, SDK package revisions and an Android CLI version guard. Command-line-tools 23 delegates to Android CLI 1.0.16261425; use its documented SDK installer with explicit @revision and metrics disabled. No AI inference, login, device or emulator commands are involved. Local/CI use the same installer. Runtime build reads the generated ignored env.sh. No Android product config changed.

Current progress checkpoint: three tool archives downloaded and verified, SDK package download/install in progress locally. Build/tests NOT yet claimed passed. Workflow syntax passes actionlint. Local process log: /tmp/pb-f2-setup.log and .tooling/pipeline-android/sdk-install.log; intended next command scripts/pipeline-android, then checkpoint F2 and scripts/project-pipeline check-full.

## CI evidence and local storage adjustment

Run 35135773943 / Android job 104927647159 successfully installed the locked official environment and assembled the debug APK. Failure then exposed an incorrect pipeline task: :app:testDebugUnitTest discovers zero JUnit tests. The only src/test file is the standalone ControllerLeaseStateNameTest.kt with a main method and five checks; its existing canonical Gradle task is :app:testControllerLeaseStateName. The pipeline now invokes that real task (which depends on test compilation), plus assembleDebug and lintDebug. No test source or test assertions changed; no failOnNoDiscoveredTests override introduced.

Local /home ran out of space downloading NDK. Only newly created pipeline tooling and Cargo target caches were relocated to /var/tmp/phoneboost-p0-fo4lub9x, which has over 140 GiB free, via ignored symlinks in this clone. The installer resolves that path and puts Gradle caches there too. No old clone or operator data touched. SDK download resumed; local build validation remains pending.

Same CI run revealed a separate existing Rust test failure: auto_use::tests::remote_blake3_64_mib_is_exact_when_practical, fallback TransportLost rather than RemoteSuccess, in unchanged auto_use.rs:3834. The preceding F1 CI run passed the whole Rust job; all hs_t20/21/22 remain green. No unrelated test/runtime modification or retry-to-green applied. Record the final full check outcome honestly.

## F2 targeted CI validation — PASS

Run https://github.com/lilugemusikmaking-design/phoneboost/actions/runs/35136807841 at 6044c061fa3270e2ff0160374551721a4b880044: Android job 104931120331 PASS. Official toolchain install, aarch64 Rust/JNI build, assembleDebug, existing standalone ControllerLeaseStateNameTest (five assertions), and lintDebug completed. Thus original API37 availability blocker is resolved without altering Android product configuration. CI evidence: f2-ci-results.json. Local provisioning is completing the same pinned packages; the mandatory check-full will record the local outcome separately. No physical proof or readiness transition.
